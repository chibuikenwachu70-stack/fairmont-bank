<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Pay Bills | Fairmont Bank</title>

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/style.css') }}"
    >

    <style>

        * {
            box-sizing: border-box;
        }

        body {

            margin: 0;

            min-height: 100vh;

            font-family:
                Inter,
                -apple-system,
                BlinkMacSystemFont,
                "Segoe UI",
                sans-serif;

            background:
                radial-gradient(
                    circle at top right,
                    rgba(61, 91, 145, 0.22),
                    transparent 38%
                ),
                #07111f;

            color: #ffffff;
        }


        .bills-page {

            min-height: 100vh;

            padding:
                22px 18px 40px;
        }


        .bills-container {

            width:
                min(720px, 100%);

            margin:
                0 auto;
        }


        .bills-topbar {

            display: flex;

            align-items: center;

            justify-content:
                space-between;

            gap: 16px;

            margin-bottom:
                30px;
        }


        .back-link {

            color:
                rgba(255,255,255,0.72);

            text-decoration:
                none;

            font-size:
                14px;

            font-weight:
                600;
        }


        .back-link:hover {

            color:
                #ffffff;
        }


        .brand-small {

            display:
                flex;

            align-items:
                center;

            gap:
                9px;
        }


        .brand-mark {

            width:
                34px;

            height:
                34px;

            border-radius:
                10px;

            display:
                flex;

            align-items:
                center;

            justify-content:
                center;

            background:
                rgba(255,255,255,0.08);

            border:
                1px solid
                rgba(255,255,255,0.10);

            font-weight:
                700;
        }


        .brand-name {

            display:
                flex;

            flex-direction:
                column;

            line-height:
                1;
        }


        .brand-name strong {

            font-size:
                12px;

            letter-spacing:
                2px;
        }


        .brand-name span {

            margin-top:
                3px;

            font-size:
                8px;

            letter-spacing:
                3px;

            opacity:
                0.58;
        }


        .bills-heading {

            margin-bottom:
                24px;
        }


        .eyebrow {

            margin:
                0 0 9px;

            font-size:
                10px;

            font-weight:
                700;

            letter-spacing:
                2px;

            text-transform:
                uppercase;

            color:
                rgba(255,255,255,0.46);
        }


        h1 {

            margin:
                0;

            font-size:
                clamp(30px, 7vw, 42px);

            line-height:
                1.08;

            letter-spacing:
                -1px;
        }


        .intro {

            margin:
                12px 0 0;

            color:
                rgba(255,255,255,0.60);

            font-size:
                14px;

            line-height:
                1.6;
        }


        .balance-card {

            margin-bottom:
                16px;

            padding:
                17px 18px;

            border-radius:
                17px;

            background:
                rgba(255,255,255,0.055);

            border:
                1px solid
                rgba(255,255,255,0.08);
        }


        .balance-label {

            display:
                block;

            margin-bottom:
                5px;

            color:
                rgba(255,255,255,0.43);

            font-size:
                11px;

            text-transform:
                uppercase;

            letter-spacing:
                1.4px;
        }


        .balance-value {

            font-size:
                24px;

            font-weight:
                700;
        }


        .bill-form {

            padding:
                20px;

            border-radius:
                22px;

            background:
                linear-gradient(
                    145deg,
                    rgba(255,255,255,0.095),
                    rgba(255,255,255,0.045)
                );

            border:
                1px solid
                rgba(255,255,255,0.09);

            box-shadow:
                0 18px 45px
                rgba(0,0,0,0.20);
        }


        .field {

            margin-bottom:
                17px;
        }


        label {

            display:
                block;

            margin-bottom:
                8px;

            color:
                rgba(255,255,255,0.75);

            font-size:
                12px;

            font-weight:
                600;
        }


        select,
        input {

            width:
                100%;

            height:
                50px;

            padding:
                0 14px;

            border-radius:
                13px;

            border:
                1px solid
                rgba(255,255,255,0.10);

            outline:
                none;

            background:
                rgba(0,0,0,0.20);

            color:
                #ffffff;

            font-size:
                14px;
        }


        select:focus,
        input:focus {

            border-color:
                rgba(255,255,255,0.27);
        }


        select option {

            background:
                #101d2e;

            color:
                #ffffff;
        }


        .field-help {

            margin:
                7px 0 0;

            color:
                rgba(255,255,255,0.38);

            font-size:
                11px;
        }


        .errors {

            margin-bottom:
                18px;

            padding:
                14px 15px;

            border-radius:
                13px;

            background:
                rgba(180,50,50,0.14);

            border:
                1px solid
                rgba(220,90,90,0.22);

            color:
                rgba(255,220,220,0.88);

            font-size:
                13px;

            line-height:
                1.6;
        }


        .errors div + div {

            margin-top:
                4px;
        }


        .submit-button {

            width:
                100%;

            min-height:
                52px;

            border:
                0;

            border-radius:
                14px;

            cursor:
                pointer;

            background:
                #ffffff;

            color:
                #07111f;

            font-size:
                14px;

            font-weight:
                700;
        }


        .submit-button:hover {

            opacity:
                0.92;
        }


        .security-note {

            margin-top:
                16px;

            text-align:
                center;

            color:
                rgba(255,255,255,0.35);

            font-size:
                11px;

            line-height:
                1.5;
        }


        @media (max-width: 500px) {

            .bills-page {

                padding:
                    18px 14px 32px;
            }

            .bill-form {

                padding:
                    17px;
            }

        }

    </style>

</head>


<body>

<main class="bills-page">

    <div class="bills-container">


        <div class="bills-topbar">

            <a
                href="{{ url_for('dashboard') }}"
                class="back-link"
            >
                ← Services
            </a>


            <div class="brand-small">

                <div class="brand-mark">
                    F
                </div>

                <div class="brand-name">

                    <strong>
                        FAIRMONT
                    </strong>

                    <span>
                        BANK
                    </span>

                </div>

            </div>

        </div>


        <section class="bills-heading">

            <p class="eyebrow">
                Everyday Banking
            </p>

            <h1>
                Pay Bills
            </h1>

            <p class="intro">
                Make a bill payment from your Fairmont Bank account
                using the secure payment form below.
            </p>

        </section>


        <div class="balance-card">

            <span class="balance-label">
                Available balance
            </span>

            <div class="balance-value">
                £{{ "{:,.2f}".format(customer.account_balance or 0) }}
            </div>

        </div>


        {% if errors %}

        <div class="errors">

            {% for error in errors %}

                <div>
                    {{ error }}
                </div>

            {% endfor %}

        </div>

        {% endif %}


        <form
            method="POST"
            action="{{ url_for('pay_bills') }}"
            class="bill-form"
        >


            <div class="field">

                <label for="bill_category">
                    Bill category
                </label>

                <select
                    id="bill_category"
                    name="bill_category"
                    required
                >

                    <option value="">
                        Select bill category
                    </option>

                    {% for category in [
                        "Electricity",
                        "Gas",
                        "Water",
                        "Council Tax",
                        "Broadband",
                        "Mobile Phone",
                        "Landline",
                        "TV & Entertainment",
                        "Other Bills"
                    ] %}

                    <option
                        value="{{ category }}"
                        {% if form is defined and form.get('bill_category') == category %}
                            selected
                        {% endif %}
                    >
                        {{ category }}
                    </option>

                    {% endfor %}

                </select>

            </div>


            <div class="field">

                <label for="provider">
                    Bill provider
                </label>

                <input
                    type="text"
                    id="provider"
                    name="provider"
                    value="{{ form.get('provider', '') if form is defined else '' }}"
                    placeholder="Enter provider or biller"
                    autocomplete="organization"
                    required
                >

                <p class="field-help">
                    Enter the company or organisation receiving the payment.
                </p>

            </div>


            <div class="field">

                <label for="account_reference">
                    Customer / account reference
                </label>

                <input
                    type="text"
                    id="account_reference"
                    name="account_reference"
                    value="{{ form.get('account_reference', '') if form is defined else '' }}"
                    placeholder="Enter your bill account reference"
                    required
                >

            </div>


            <div class="field">

                <label for="amount">
                    Payment amount
                </label>

                <input
                    type="number"
                    id="amount"
                    name="amount"
                    value="{{ form.get('amount', '') if form is defined else '' }}"
                    placeholder="0.00"
                    min="0.01"
                    step="0.01"
                    inputmode="decimal"
                    required
                >

            </div>


            <div class="field">

                <label for="payment_reference">
                    Payment reference
                    <span style="opacity:0.45;">
                        (optional)
                    </span>
                </label>

                <input
                    type="text"
                    id="payment_reference"
                    name="payment_reference"
                    value="{{ form.get('payment_reference', '') if form is defined else '' }}"
                    placeholder="Optional payment reference"
                    maxlength="255"
                >

            </div>


            <button
                type="submit"
                class="submit-button"
            >
                Continue Payment
            </button>


        </form>


        <div class="security-note">
            Your payment is recorded securely in your Fairmont
            transaction history after successful processing.
        </div>


    </div>

</main>

</body>

</html>
